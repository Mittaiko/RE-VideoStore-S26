## Question 2
Before refactoring, unit tests need to be set up.  Most of this is completed, but the unit testing for the class ```Rental``` is not done.  Finish this, following the examples from the other unit tests.  At this point you may also add test cases to the other unit tests.

**Start Day/Time:**  04/09/2026 3:30pm

**How did you decide whether the AI’s output was useful, accurate, or appropriate?**

To evaluate the AI's output, I ocmpared the generated unit tests with the existing test files for the customer and movie classes to make sure they followed the same structure and conventions. I also reviewed the login in the Rental.get_charge() method 

**What changes did you make to the AI‑generated material? Be specific.**

I reviewed the AI-generated test cases and verified that the expected charge values matched the pricing logic. In addition to the tests, I added two additional boundary test cases: test_regular_boundary_plus_one and test_childrens_boundary_plus_one. These tests check the behavior immediately after the pricing thresholds (3 days for regular movies and 4 days for children’s movies) to ensure that the additional charges begin on the correct day. I added these tests because boundary transitions are common points where errors can occur during refactoring, so verifying the first day where extra charges apply helps ensure the behavior remains correct.

**I copied my AI-assisted chat log to ./docs/log2.txt**
[X] Yes [ ] No 

**Summarize how many test cases you added for Rental and how many extra ones you added for the rest.**

I added 10 unit test cases for the Rental class based on the examples generated by the AI assistant. These tests cover different movie types (regular, new release, and children’s movies) and include important boundary conditions where pricing rules change.

In addition, I added 2 extra test cases: test_regular_boundary_plus_one and test_childrens_boundary_plus_one. These tests check the behavior immediately after the pricing thresholds (3 days for regular movies and 4 days for children’s movies). They ensure that the additional charges begin at the correct time, which is an important edge case that could easily break during refactoring. I did not add additional test cases to the other unit test files.

**Difficulty Level:**
[ ] Easy  [X] Avg  [ ] Difficult

**End Day/Time:** 04/09/2026 4:40pm

